# scripts/local_dev

Scripts to bring up the full stack locally for development.

## Planned
- `bootstrap.sh` — install Go, Flutter, buf, golangci-lint at the right versions
- `up.sh` — start cloud-api + a local postgres + a local-store-server + seed a tenant
- `seed-tenant.sh` — create test tenant, JWT, sample products
- `tail-events.sh` — live-tail operations_log for debugging sync

## Status
Empty.
