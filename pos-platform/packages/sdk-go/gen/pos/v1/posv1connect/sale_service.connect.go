// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: pos/v1/sale_service.proto

package posv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/mibjas/pos-platform/packages/sdk-go/gen/pos/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SaleServiceName is the fully-qualified name of the SaleService service.
	SaleServiceName = "pos.v1.SaleService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SaleServiceFinalizeProcedure is the fully-qualified name of the SaleService's Finalize RPC.
	SaleServiceFinalizeProcedure = "/pos.v1.SaleService/Finalize"
	// SaleServiceGetSaleProcedure is the fully-qualified name of the SaleService's GetSale RPC.
	SaleServiceGetSaleProcedure = "/pos.v1.SaleService/GetSale"
)

// SaleServiceClient is a client for the pos.v1.SaleService service.
type SaleServiceClient interface {
	Finalize(context.Context, *connect.Request[v1.FinalizeRequest]) (*connect.Response[v1.FinalizeResponse], error)
	// GetSale is the read-side lookup for a finalized sale. Returns
	// NotFound if neither key resolves. The caller picks the key — UI
	// typically uses invoice_number (operator-facing); reversal flows
	// already hold sale_id and use that.
	GetSale(context.Context, *connect.Request[v1.GetSaleRequest]) (*connect.Response[v1.GetSaleResponse], error)
}

// NewSaleServiceClient constructs a client for the pos.v1.SaleService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSaleServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SaleServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	saleServiceMethods := v1.File_pos_v1_sale_service_proto.Services().ByName("SaleService").Methods()
	return &saleServiceClient{
		finalize: connect.NewClient[v1.FinalizeRequest, v1.FinalizeResponse](
			httpClient,
			baseURL+SaleServiceFinalizeProcedure,
			connect.WithSchema(saleServiceMethods.ByName("Finalize")),
			connect.WithClientOptions(opts...),
		),
		getSale: connect.NewClient[v1.GetSaleRequest, v1.GetSaleResponse](
			httpClient,
			baseURL+SaleServiceGetSaleProcedure,
			connect.WithSchema(saleServiceMethods.ByName("GetSale")),
			connect.WithClientOptions(opts...),
		),
	}
}

// saleServiceClient implements SaleServiceClient.
type saleServiceClient struct {
	finalize *connect.Client[v1.FinalizeRequest, v1.FinalizeResponse]
	getSale  *connect.Client[v1.GetSaleRequest, v1.GetSaleResponse]
}

// Finalize calls pos.v1.SaleService.Finalize.
func (c *saleServiceClient) Finalize(ctx context.Context, req *connect.Request[v1.FinalizeRequest]) (*connect.Response[v1.FinalizeResponse], error) {
	return c.finalize.CallUnary(ctx, req)
}

// GetSale calls pos.v1.SaleService.GetSale.
func (c *saleServiceClient) GetSale(ctx context.Context, req *connect.Request[v1.GetSaleRequest]) (*connect.Response[v1.GetSaleResponse], error) {
	return c.getSale.CallUnary(ctx, req)
}

// SaleServiceHandler is an implementation of the pos.v1.SaleService service.
type SaleServiceHandler interface {
	Finalize(context.Context, *connect.Request[v1.FinalizeRequest]) (*connect.Response[v1.FinalizeResponse], error)
	// GetSale is the read-side lookup for a finalized sale. Returns
	// NotFound if neither key resolves. The caller picks the key — UI
	// typically uses invoice_number (operator-facing); reversal flows
	// already hold sale_id and use that.
	GetSale(context.Context, *connect.Request[v1.GetSaleRequest]) (*connect.Response[v1.GetSaleResponse], error)
}

// NewSaleServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSaleServiceHandler(svc SaleServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	saleServiceMethods := v1.File_pos_v1_sale_service_proto.Services().ByName("SaleService").Methods()
	saleServiceFinalizeHandler := connect.NewUnaryHandler(
		SaleServiceFinalizeProcedure,
		svc.Finalize,
		connect.WithSchema(saleServiceMethods.ByName("Finalize")),
		connect.WithHandlerOptions(opts...),
	)
	saleServiceGetSaleHandler := connect.NewUnaryHandler(
		SaleServiceGetSaleProcedure,
		svc.GetSale,
		connect.WithSchema(saleServiceMethods.ByName("GetSale")),
		connect.WithHandlerOptions(opts...),
	)
	return "/pos.v1.SaleService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SaleServiceFinalizeProcedure:
			saleServiceFinalizeHandler.ServeHTTP(w, r)
		case SaleServiceGetSaleProcedure:
			saleServiceGetSaleHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSaleServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSaleServiceHandler struct{}

func (UnimplementedSaleServiceHandler) Finalize(context.Context, *connect.Request[v1.FinalizeRequest]) (*connect.Response[v1.FinalizeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pos.v1.SaleService.Finalize is not implemented"))
}

func (UnimplementedSaleServiceHandler) GetSale(context.Context, *connect.Request[v1.GetSaleRequest]) (*connect.Response[v1.GetSaleResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pos.v1.SaleService.GetSale is not implemented"))
}
