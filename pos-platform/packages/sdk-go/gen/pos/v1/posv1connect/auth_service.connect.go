// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: pos/v1/auth_service.proto

package posv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/mibjas/pos-platform/packages/sdk-go/gen/pos/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AuthServiceName is the fully-qualified name of the AuthService service.
	AuthServiceName = "pos.v1.AuthService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AuthServiceRegisterDeviceProcedure is the fully-qualified name of the AuthService's
	// RegisterDevice RPC.
	AuthServiceRegisterDeviceProcedure = "/pos.v1.AuthService/RegisterDevice"
	// AuthServiceLoginProcedure is the fully-qualified name of the AuthService's Login RPC.
	AuthServiceLoginProcedure = "/pos.v1.AuthService/Login"
)

// AuthServiceClient is a client for the pos.v1.AuthService service.
type AuthServiceClient interface {
	// RegisterDevice provisions THIS terminal. Manager authenticates inline;
	// requires the `owner` role. Returns the device_secret exactly once.
	RegisterDevice(context.Context, *connect.Request[v1.RegisterDeviceRequest]) (*connect.Response[v1.RegisterDeviceResponse], error)
	// Login authenticates a user on an already-registered device and mints a
	// session token. No refresh token by design — re-login at expiry.
	Login(context.Context, *connect.Request[v1.LoginRequest]) (*connect.Response[v1.LoginResponse], error)
}

// NewAuthServiceClient constructs a client for the pos.v1.AuthService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAuthServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AuthServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	authServiceMethods := v1.File_pos_v1_auth_service_proto.Services().ByName("AuthService").Methods()
	return &authServiceClient{
		registerDevice: connect.NewClient[v1.RegisterDeviceRequest, v1.RegisterDeviceResponse](
			httpClient,
			baseURL+AuthServiceRegisterDeviceProcedure,
			connect.WithSchema(authServiceMethods.ByName("RegisterDevice")),
			connect.WithClientOptions(opts...),
		),
		login: connect.NewClient[v1.LoginRequest, v1.LoginResponse](
			httpClient,
			baseURL+AuthServiceLoginProcedure,
			connect.WithSchema(authServiceMethods.ByName("Login")),
			connect.WithClientOptions(opts...),
		),
	}
}

// authServiceClient implements AuthServiceClient.
type authServiceClient struct {
	registerDevice *connect.Client[v1.RegisterDeviceRequest, v1.RegisterDeviceResponse]
	login          *connect.Client[v1.LoginRequest, v1.LoginResponse]
}

// RegisterDevice calls pos.v1.AuthService.RegisterDevice.
func (c *authServiceClient) RegisterDevice(ctx context.Context, req *connect.Request[v1.RegisterDeviceRequest]) (*connect.Response[v1.RegisterDeviceResponse], error) {
	return c.registerDevice.CallUnary(ctx, req)
}

// Login calls pos.v1.AuthService.Login.
func (c *authServiceClient) Login(ctx context.Context, req *connect.Request[v1.LoginRequest]) (*connect.Response[v1.LoginResponse], error) {
	return c.login.CallUnary(ctx, req)
}

// AuthServiceHandler is an implementation of the pos.v1.AuthService service.
type AuthServiceHandler interface {
	// RegisterDevice provisions THIS terminal. Manager authenticates inline;
	// requires the `owner` role. Returns the device_secret exactly once.
	RegisterDevice(context.Context, *connect.Request[v1.RegisterDeviceRequest]) (*connect.Response[v1.RegisterDeviceResponse], error)
	// Login authenticates a user on an already-registered device and mints a
	// session token. No refresh token by design — re-login at expiry.
	Login(context.Context, *connect.Request[v1.LoginRequest]) (*connect.Response[v1.LoginResponse], error)
}

// NewAuthServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAuthServiceHandler(svc AuthServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	authServiceMethods := v1.File_pos_v1_auth_service_proto.Services().ByName("AuthService").Methods()
	authServiceRegisterDeviceHandler := connect.NewUnaryHandler(
		AuthServiceRegisterDeviceProcedure,
		svc.RegisterDevice,
		connect.WithSchema(authServiceMethods.ByName("RegisterDevice")),
		connect.WithHandlerOptions(opts...),
	)
	authServiceLoginHandler := connect.NewUnaryHandler(
		AuthServiceLoginProcedure,
		svc.Login,
		connect.WithSchema(authServiceMethods.ByName("Login")),
		connect.WithHandlerOptions(opts...),
	)
	return "/pos.v1.AuthService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AuthServiceRegisterDeviceProcedure:
			authServiceRegisterDeviceHandler.ServeHTTP(w, r)
		case AuthServiceLoginProcedure:
			authServiceLoginHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAuthServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAuthServiceHandler struct{}

func (UnimplementedAuthServiceHandler) RegisterDevice(context.Context, *connect.Request[v1.RegisterDeviceRequest]) (*connect.Response[v1.RegisterDeviceResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pos.v1.AuthService.RegisterDevice is not implemented"))
}

func (UnimplementedAuthServiceHandler) Login(context.Context, *connect.Request[v1.LoginRequest]) (*connect.Response[v1.LoginResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pos.v1.AuthService.Login is not implemented"))
}
