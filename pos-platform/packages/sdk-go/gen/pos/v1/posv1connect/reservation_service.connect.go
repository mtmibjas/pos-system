// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: pos/v1/reservation_service.proto

package posv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/mibjas/pos-platform/packages/sdk-go/gen/pos/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ReservationServiceName is the fully-qualified name of the ReservationService service.
	ReservationServiceName = "pos.v1.ReservationService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ReservationServiceReserveProcedure is the fully-qualified name of the ReservationService's
	// Reserve RPC.
	ReservationServiceReserveProcedure = "/pos.v1.ReservationService/Reserve"
	// ReservationServiceReleaseProcedure is the fully-qualified name of the ReservationService's
	// Release RPC.
	ReservationServiceReleaseProcedure = "/pos.v1.ReservationService/Release"
)

// ReservationServiceClient is a client for the pos.v1.ReservationService service.
type ReservationServiceClient interface {
	// Reserve creates a new active hold or fails with FailedPrecondition if
	// available stock is insufficient (after lazily expiring stale holds).
	Reserve(context.Context, *connect.Request[v1.ReserveRequest]) (*connect.Response[v1.ReserveResponse], error)
	// Release cancels an active hold. Idempotent on already-released rows.
	Release(context.Context, *connect.Request[v1.ReleaseRequest]) (*connect.Response[v1.ReleaseResponse], error)
}

// NewReservationServiceClient constructs a client for the pos.v1.ReservationService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewReservationServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ReservationServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	reservationServiceMethods := v1.File_pos_v1_reservation_service_proto.Services().ByName("ReservationService").Methods()
	return &reservationServiceClient{
		reserve: connect.NewClient[v1.ReserveRequest, v1.ReserveResponse](
			httpClient,
			baseURL+ReservationServiceReserveProcedure,
			connect.WithSchema(reservationServiceMethods.ByName("Reserve")),
			connect.WithClientOptions(opts...),
		),
		release: connect.NewClient[v1.ReleaseRequest, v1.ReleaseResponse](
			httpClient,
			baseURL+ReservationServiceReleaseProcedure,
			connect.WithSchema(reservationServiceMethods.ByName("Release")),
			connect.WithClientOptions(opts...),
		),
	}
}

// reservationServiceClient implements ReservationServiceClient.
type reservationServiceClient struct {
	reserve *connect.Client[v1.ReserveRequest, v1.ReserveResponse]
	release *connect.Client[v1.ReleaseRequest, v1.ReleaseResponse]
}

// Reserve calls pos.v1.ReservationService.Reserve.
func (c *reservationServiceClient) Reserve(ctx context.Context, req *connect.Request[v1.ReserveRequest]) (*connect.Response[v1.ReserveResponse], error) {
	return c.reserve.CallUnary(ctx, req)
}

// Release calls pos.v1.ReservationService.Release.
func (c *reservationServiceClient) Release(ctx context.Context, req *connect.Request[v1.ReleaseRequest]) (*connect.Response[v1.ReleaseResponse], error) {
	return c.release.CallUnary(ctx, req)
}

// ReservationServiceHandler is an implementation of the pos.v1.ReservationService service.
type ReservationServiceHandler interface {
	// Reserve creates a new active hold or fails with FailedPrecondition if
	// available stock is insufficient (after lazily expiring stale holds).
	Reserve(context.Context, *connect.Request[v1.ReserveRequest]) (*connect.Response[v1.ReserveResponse], error)
	// Release cancels an active hold. Idempotent on already-released rows.
	Release(context.Context, *connect.Request[v1.ReleaseRequest]) (*connect.Response[v1.ReleaseResponse], error)
}

// NewReservationServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewReservationServiceHandler(svc ReservationServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	reservationServiceMethods := v1.File_pos_v1_reservation_service_proto.Services().ByName("ReservationService").Methods()
	reservationServiceReserveHandler := connect.NewUnaryHandler(
		ReservationServiceReserveProcedure,
		svc.Reserve,
		connect.WithSchema(reservationServiceMethods.ByName("Reserve")),
		connect.WithHandlerOptions(opts...),
	)
	reservationServiceReleaseHandler := connect.NewUnaryHandler(
		ReservationServiceReleaseProcedure,
		svc.Release,
		connect.WithSchema(reservationServiceMethods.ByName("Release")),
		connect.WithHandlerOptions(opts...),
	)
	return "/pos.v1.ReservationService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ReservationServiceReserveProcedure:
			reservationServiceReserveHandler.ServeHTTP(w, r)
		case ReservationServiceReleaseProcedure:
			reservationServiceReleaseHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedReservationServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedReservationServiceHandler struct{}

func (UnimplementedReservationServiceHandler) Reserve(context.Context, *connect.Request[v1.ReserveRequest]) (*connect.Response[v1.ReserveResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pos.v1.ReservationService.Reserve is not implemented"))
}

func (UnimplementedReservationServiceHandler) Release(context.Context, *connect.Request[v1.ReleaseRequest]) (*connect.Response[v1.ReleaseResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pos.v1.ReservationService.Release is not implemented"))
}
