# mobile-owner

Mobile dashboard for store owners (iOS + Android).

## Talks to
`cloud-api` only — read-only dashboards backed by OpenAPI clients. **Never** the local store server.

## Status
Scaffold only — will be implemented in Phase 5 (Accounting & Reports).
